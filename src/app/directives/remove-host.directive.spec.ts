import { RemoveHostDirective } from './remove-host.directive';

describe('RemoveHostDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveHostDirective();
    expect(directive).toBeTruthy();
  });
});
