import { EditTextDirective } from './edit-text.directive';

describe('EditTextDirective', () => {
  it('should create an instance', () => {
    const directive = new EditTextDirective();
    expect(directive).toBeTruthy();
  });
});
