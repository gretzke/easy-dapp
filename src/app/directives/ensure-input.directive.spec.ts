import { EnsureInputDirective } from './ensure-input.directive';

describe('EnsureInputDirective', () => {
  it('should create an instance', () => {
    const directive = new EnsureInputDirective();
    expect(directive).toBeTruthy();
  });
});
