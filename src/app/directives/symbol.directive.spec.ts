import { SymbolDirective } from './symbol.directive';

describe('SymbolDirective', () => {
  it('should create an instance', () => {
    const directive = new SymbolDirective();
    expect(directive).toBeTruthy();
  });
});
